# Magnific Kling Motion Control desktop app
# The Magnific API key is stored in electron-store (managed via the in-app
# Settings page), so no environment variables are required at runtime.
# This file is intentionally empty and exists only for parity with the spec.
